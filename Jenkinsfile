pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'uname -a'
                sh 'echo main branch!'
		sh 'echo this is a testing branch!!'
            }
        }
    }
}
